- code should use imported object directly, for hot reloading to work:
let bar = require("./bar.js");
foo(bar.value);

- option to obfuscate class names in class metadata?

concept:
- write my own TeaVmTarget that delegates to JavaScriptTarget for EVERY class.
- hammer JavaScriptTarget output so:
-- it adds proper require() at the top
-- it uses imported stdlib instead of emitted one:
stdlib = require(....)
$rt_nextId = stdlib.$rt_nextId;
// ......
-- it exports symbols used by other classes
-- rt_packages and rt_metadata should work as-is


problems:
- teavm runtime depends on JRE
- not all teavm runtime names start with $rt_ (e.g. Long_xxx functions)

ISSUES
- @JsBody only works in current class because teavm creates $js_body$ proxy in class which defined @JsBody
   It does not work in split compilation because class hosting JsBody has no idea it needs to emit proxies
   It works if JsBody declaration and usage is in the same class however. Probably due to inlining.
- DONE AliasProvider: "uniqueness" stuff does not work - it is NOT stable across multiple class compilations
    persist AliasProvider state between invocations, removing uniqueness suffix generation won't work - it is heavily used
- DONE calling the tool from gradle plugin: somehow it persists state between invocations breaking compilation. Investigate.